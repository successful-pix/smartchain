import { createFileRoute } from "@tanstack/react-router";
import { ArrowLeft } from "lucide-react";
import { useProfile, usePreferences } from "@/hooks/useWalletData";
import { supabase } from "@/integrations/supabase/client";
export const Route = createFileRoute("/account")({ component: Account });
function Account(){const profile=useProfile();const prefs=usePreferences();async function signOut(){await supabase.auth.signOut();window.location.href="/auth";}return <main className="mx-auto max-w-lg px-4 pt-5 pb-24"><a href="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft size={17}/> Dashboard</a><h1 className="mt-5 text-2xl font-semibold">Account</h1><div className="mt-5 space-y-3 rounded-2xl border border-border bg-card p-5"><div><p className="text-xs text-muted-foreground">Name</p><p className="font-medium">{profile.data?.display_name??"SmartChain user"}</p></div><div><p className="text-xs text-muted-foreground">Currency</p><p className="font-medium">{prefs.data?.currency??"USD"}</p></div><div><p className="text-xs text-muted-foreground">Security notifications</p><p className="font-medium">{prefs.data?.notify_security?"Enabled":"Disabled"}</p></div><button onClick={()=>void signOut()} className="mt-3 w-full rounded-lg border border-destructive/30 px-4 py-3 text-sm font-medium text-destructive">Sign out</button></div></main>}
